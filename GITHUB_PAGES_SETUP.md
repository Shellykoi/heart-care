# GitHub Pages 部署配置指南

## ✅ 已完成

1. ✅ 构建文件已复制到根目录
2. ✅ 代码已推送到 GitHub
3. ✅ 创建了部署脚本（`deploy.bat` 和 `deploy.sh`）

## 📋 下一步：配置 GitHub Pages

### 1. 在 GitHub 仓库中启用 GitHub Pages

1. 访问你的仓库：https://github.com/Shellykoi/heart-care
2. 点击 **Settings**（设置）
3. 在左侧菜单中找到 **Pages**（页面）
4. 在 **Source**（源）部分：
   - 选择 **Deploy from a branch**（从分支部署）
   - **Branch** 选择 `main` 或 `master`
   - **Folder** 选择 `/ (root)`（根目录）
5. 点击 **Save**（保存）

### 2. 等待部署完成

- GitHub Pages 通常需要 1-2 分钟来部署
- 你可以在仓库的 **Actions** 标签页查看部署状态（如果有 GitHub Actions 工作流）
- 或者直接访问：https://shellykoi.github.io/heart-care/

### 3. 验证部署

访问以下链接验证部署是否成功：
- **主页**：https://shellykoi.github.io/heart-care/
- **登录页面**：https://shellykoi.github.io/heart-care/login

## 🔄 后续更新流程

每次更新代码后，执行以下步骤：

### Windows 用户

```bash
# 1. 构建项目
npm run build

# 2. 复制 build 文件到根目录
xcopy /E /Y /I build\* .

# 3. 提交并推送
git add index.html 404.html assets/
git commit -m "deploy: update GitHub Pages"
git push origin main
```

或者直接运行部署脚本：
```bash
deploy.bat
```

### Linux/Mac 用户

```bash
# 1. 构建项目
npm run build

# 2. 复制 build 文件到根目录
cp -r build/* .

# 3. 提交并推送
git add index.html 404.html assets/
git commit -m "deploy: update GitHub Pages"
git push origin main
```

或者直接运行部署脚本：
```bash
chmod +x deploy.sh
./deploy.sh
```

## 📁 项目结构

部署后的根目录结构应该是：

```
heart-care/
├── index.html          # 主页面（从 build/ 复制）
├── 404.html            # 404 页面（用于 SPA 路由）
├── assets/             # 静态资源（从 build/assets/ 复制）
│   ├── index-*.css
│   ├── index-*.js
│   └── *.png
├── src/                # 源代码（不部署）
├── build/              # 构建输出（不部署）
└── ...
```

## ⚠️ 重要提示

1. **不要删除根目录的 `index.html`、`404.html` 和 `assets/` 目录**
   - 这些是 GitHub Pages 需要的文件

2. **`.gitignore` 配置**
   - `build/` 目录在 `.gitignore` 中，不会被提交
   - 但根目录的 `index.html`、`404.html` 和 `assets/` 需要提交

3. **Base Path 配置**
   - Vite 配置中的 `base: '/heart-care/'` 已正确设置
   - 这确保所有资源路径都使用正确的 base path

4. **CORS 配置**
   - 后端已配置允许来自 `https://shellykoi.github.io` 的请求
   - 前端会自动检测生产环境并使用正确的后端 URL

## 🐛 故障排除

### 问题：GitHub Pages 显示 404

**解决方案**：
1. 检查 GitHub Pages 设置是否正确（Source 选择根目录）
2. 确认根目录有 `index.html` 文件
3. 等待几分钟让 GitHub 完成部署
4. 清除浏览器缓存后重试

### 问题：页面显示但样式/脚本加载失败

**解决方案**：
1. 检查 `index.html` 中的资源路径是否正确（应该是 `/heart-care/assets/...`）
2. 确认 `assets/` 目录已正确提交到 GitHub
3. 检查浏览器控制台的错误信息

### 问题：API 请求失败

**解决方案**：
1. 检查后端服务是否正常运行：https://heart-care-m28z.onrender.com/api/health
2. 检查浏览器控制台是否有 CORS 错误
3. 确认前端 API 配置正确（自动检测生产环境）

## 📞 支持

如果遇到问题，请检查：
- GitHub Pages 设置
- 浏览器控制台错误
- 网络连接
- 后端服务状态

