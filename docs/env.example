# Vite environment variables for heart-care frontend
# Copy this file to the project root as .env.development and/or .env.production,
# or create environment-specific variants (e.g. .env.development.local).

# Base URL of the backend API (include the /api suffix if your backend exposes it)
VITE_API_BASE_URL=http://localhost:8000/api

